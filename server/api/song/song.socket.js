/**
 * Broadcast updates to client when the model changes
 */

'use strict';

// var lyrics = require('./lyrics.model');

// exports.register = function(socket) {
//   lyrics.schema.post('save', function (doc) {
//     onSave(socket, doc);
//   });
//   lyrics.schema.post('remove', function (doc) {
//     onRemove(socket, doc);
//   });
// };

// function onSave(socket, doc, cb) {
//   socket.emit('lyrics:save', doc);
// }
//
// function onRemove(socket, doc, cb) {
//   socket.emit('lyrics:remove', doc);
// }
